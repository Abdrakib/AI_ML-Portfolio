"""
Instance Segmentation
Segments individual object instances
"""

import cv2
import numpy as np

# using Mask R-CNN or similar
# from mrcnn import utils, model as modellib

def detect_instances(image):
    """Detect and segment instances"""
    # results = model.detect([image], verbose=1)
    # masks = results[0]['masks']
    # return masks
    return image

print("Instance segmentation script ready. Use Mask R-CNN for implementation.")
